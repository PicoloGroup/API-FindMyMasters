version: '3'
services:
  findmymasters-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: findmymasters-api
    restart: always
    ports:
      - 3000:8081
    env_file:
      - env/production.env
